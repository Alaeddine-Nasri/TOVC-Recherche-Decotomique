#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "fonctions.c"

int main()
{

    srand(time(NULL));
    printf("Hello world!\n");
   // FILE *f;
    genere();

    //recheche_deco(f);
 //   rand_string(i,s);
 //   num_to_string(12,10,s);
  //  printf("%s",s);
    return 0;
}
