#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "fonctions.c"

int main()
{
   // char s[10];
    srand(time(NULL));
    printf("Hello world!\n");
    genere();
 //   num_to_string(12,10,s);
  //  printf("%s",s);
    return 0;
}
